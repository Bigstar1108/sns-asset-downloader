name: sns_asset_downloader
description: SNS asset download app

publish_to: 'none'

version: 1.0.0

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  logger: 1.1.0
  flutter_sharing_intent: 1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
